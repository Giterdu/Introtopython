import time

# 3 second countdown program

#time.sleep(3)

#print("Time's UP!")

# countdown program with a user interface
# a while loop would work for this as well (try to figure out)

#my_time = int(input("Enter the time in seconds: "))

#for x in (range(my_time, 0, -1)): #can do this rather than reversed(range(x,y,z))
    #print(x)
    #time.sleep(1)

#print("Time's UP!")

#countown with UI with proper digits displayed 
                    #and proper double padding

#my_time = int(input("Enter the time in seconds: "))

#for x in (range(my_time, 0, -1)): #can do this rather than reversed(range(x,y,z))
    #seconds = x % 60
    #minutes = int(x / 60) % 60
    #hours = int(x / 3600) #there's 3600 seconds in an hour
    #print(f"{hours:02}:{minutes:02}:{seconds:02}") #format specifier says 2 digits and zero pad those digits 
    #time.sleep(1)

#print("Time's UP!")
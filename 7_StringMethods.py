#name = input("Enter your full name: ")
#phone_number = input("Enter your phone #: ")

#result = len(name) #Finds the character length of your string
#result = phone_number.count("-") #Counts the amount of dashes in your phone number
#result = phone_number.replace("-", "") #Replaces selected characters with whatever you choose
#print(result)

#first_result = name.find("l") #Finds the first occurance (first character is 0 in array)
#last_result = name.rfind("l") #Finds the last occurance 
#print(first_result)
#print(last_result)
#if python cannot find return character you are looking for python will return -1

#name = name.capitalize() #Makes the first character uppercase
#name = name.upper() #MAKES EVERY CHARACTER UPPERCASE
#name = name.lower() #makes every character lowercase
#print(name)

#result = name.isdigit() #uses a boolean to ask if its true that the name contains only numerical string characters ("Russell123" would be false)
#result = name.isalpha() #uses a boolean to ask if its true that the name contains only alphabetical string characters ("Russell123" would be false)

print(help(str)) #prints a comprehensive list of all of the string methods available